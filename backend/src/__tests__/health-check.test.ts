/**
 * Health Check Unit Test
 * 
 * This test verifies that the test framework (Jest) is working correctly.
 * It is a simple test that always passes and requires no mocks or external dependencies.
 */

test('health check - test framework is working', () => {
  expect(true).toBe(true);
});
